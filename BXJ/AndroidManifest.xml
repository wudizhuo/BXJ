<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.bxj"
    android:versionCode="3"
    android:versionName="1.2" >

    <uses-sdk
        android:minSdkVersion="9"
        android:targetSdkVersion="18" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.MOUNT_UNMOUNT_FILESYSTEMS" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />

    <!-- 加广告需要的一些权限 -->
    <!-- 多盟所需权限，无此权限多盟将无法展示广告 -->
    <uses-permission android:name="android.permission.VIBRATE" />

    <!-- adchina -->
    <uses-permission android:name="android.permission.CHANGE_CONFIGURATION" />

    <application
        android:name="com.bxj.AppApplication"
        android:allowBackup="true"
        android:hardwareAccelerated="true"
        android:icon="@drawable/app_icon"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name="com.bxj.activity.WelcomeActivity"
            android:label="@string/app_name" 
            android:theme="@android:style/Theme.NoTitleBar.Fullscreen"
            >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="com.bxj.activity.MainActivity" />
        <activity android:name="com.bxj.activity.SettingActivity" />
        <activity android:name="com.bxj.activity.WebContentActivity" />
        <activity android:name="com.bxj.activity.FeedBcakActivity" />
        <activity android:name="com.bxj.activity.AboutActivity" />
        <activity android:name="com.bxj.activity.SecondWebActivity" />

        <!-- 声明Mogo的Activity -->
        <!-- 声明Mogo全插屏所用的Activity -->
        <activity
            android:name="com.adsmogo.adview.AdsMogoInterstitialActivity"
            android:screenOrientation="landscape" >
        </activity>
        <activity
            android:name="com.adsmogo.adview.AdsMogoWebView"
            android:configChanges="orientation|keyboardHidden"
            android:label="@string/app_name" >
        </activity>
        <activity
            android:name="com.adsmogo.mriad.view.AdsMogoActionHandler"
            android:configChanges="orientation|keyboardHidden"
            android:label="@string/app_name" >
        </activity>

        <!-- 注册Service，自主广告与交换广告的应用下载功能所需 -->
        <service
            android:name="com.adsmogo.controller.service.UpdateService"
            android:exported="true"
            android:process=":remote" />
        <!-- 注册Service，自主广告与交换广告的应用下载功能所需 ，安装应用所用 -->
        <service
            android:name="com.adsmogo.controller.service.CountService"
            android:exported="true"
            android:process=":remote" />

        <!-- 友盟插件 -->
        <meta-data
            android:name="UMENG_APPKEY"
            android:value="52d2aecc56240b219500c777" >
        </meta-data>
        <meta-data
            android:name="UMENG_CHANNEL"
            android:value="@string/umeng_channel" />

        <!-- 自动更新服务 -->
        <service
            android:name="com.umeng.common.net.DownloadingService"
            android:process=":DownloadingService" >
        </service>

        <activity
            android:name="com.umeng.update.UpdateDialogActivity"
            android:theme="@android:style/Theme.Translucent.NoTitleBar" >
        </activity>
        <!-- 声明多盟的Activity -->
        <activity
            android:name="cn.domob.android.ads.DomobActivity"
            android:theme="@android:style/Theme.Translucent" />
        <!-- 声明AdMob的Activity -->
        <activity
            android:name="com.google.ads.AdActivity"
            android:configChanges="keyboard|keyboardHidden|orientation|screenLayout|uiMode|screenSize|smallestScreenSize" />
        <!-- 声明AdChina的Activity -->
        <activity
            android:name="com.adchina.android.ads.views.AdBrowserView"
            android:configChanges="orientation|keyboard|keyboardHidden"
            android:label="@string/app_name"
            android:theme="@android:style/Theme.Translucent" >
        </activity>
        <!-- 添加百度应用详情Activity -->
        <meta-data
            android:name="BaiduMobAd_DEBUG_TOKEN"
            android:value="baidu" />

        <activity
            android:name="com.baidu.mobads.AppActivity"
            android:configChanges="keyboard|keyboardHidden|orientation" />
    </application>

</manifest>